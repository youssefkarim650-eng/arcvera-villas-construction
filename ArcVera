import { createFileRoute } from "@tanstack/react-router";
import { useEffect, useState } from "react";
import { Mail, Phone, MapPin, ArrowRight, Calendar, Building2 } from "lucide-react";
import heroImg from "@/assets/villa-hero.jpg";
import logo from "@/assets/arcvera-logo.jpg";
import constructionImg from "@/assets/construction-management.jpg";
import interiorImg from "@/assets/interior-finishing.jpg";
import tamemImg from "@/assets/tamem-mostafa.jpg";
import mohamdImg from "@/assets/mohamd-adael.jpg";

export const Route = createFileRoute("/")({
  component: Index,
});

function Index() {
  const [splashGone, setSplashGone] = useState(false);
  const [splashFading, setSplashFading] = useState(false);

  useEffect(() => {
    const fade = setTimeout(() => setSplashFading(true), 2000);
    const remove = setTimeout(() => setSplashGone(true), 2700);
    return () => { clearTimeout(fade); clearTimeout(remove); };
  }, []);

  useEffect(() => {
    const els = document.querySelectorAll(".reveal");
    const io = new IntersectionObserver(
      (entries) => {
        entries.forEach((e) => {
          if (e.isIntersecting) {
            e.target.classList.add("is-visible");
            io.unobserve(e.target);
          }
        });
      },
      { threshold: 0.15 }
    );
    els.forEach((el) => io.observe(el));
    return () => io.disconnect();
  }, []);

  return (
    <div className="min-h-screen bg-background text-foreground">
      {/* Splash intro */}
      {!splashGone && (
        <div
          className={`fixed inset-0 z-[100] flex flex-col items-center justify-center bg-background ${splashFading ? "splash-fade-out" : ""}`}
        >
          <div className="relative">
            <span className="absolute inset-0 rounded-full border border-primary splash-ring" />
            <span className="absolute inset-0 rounded-full border border-primary splash-ring" style={{ animationDelay: "0.6s" }} />
            <img
              src={logo}
              alt="ArcVera"
              className="splash-logo h-28 w-28 md:h-36 md:w-36 object-cover rounded-sm shadow-[var(--shadow-elegant)]"
            />
          </div>
          <p className="splash-text mt-8 font-serif text-2xl md:text-3xl tracking-wide">ArcVera</p>
          <p className="splash-text text-xs uppercase tracking-[0.4em] text-muted-foreground mt-2">
            General Contracting
          </p>
        </div>
      )}

      {/* Nav */}
      <header className="fixed top-0 inset-x-0 z-50 backdrop-blur-md bg-background/70 border-b border-border/50">
        <div className="max-w-7xl mx-auto px-6 lg:px-10 h-16 flex items-center justify-between">
          <a href="#top" className="flex items-center gap-3">
            <img src={logo} alt="ArcVera logo" className="h-9 w-9 object-cover rounded-sm" />
            <span className="font-serif text-lg tracking-wide">ArcVera</span>
          </a>
          <nav className="hidden md:flex items-center gap-8 text-sm text-muted-foreground">
            <a href="#about" className="hover:text-primary transition-colors">About</a>
            <a href="#services" className="hover:text-primary transition-colors">Services</a>
            <a href="#numbers" className="hover:text-primary transition-colors">Numbers</a>
            <a href="#team" className="hover:text-primary transition-colors">Team</a>
            <a href="#contact" className="hover:text-primary transition-colors">Contact</a>
          </nav>
          <a
            href="#contact"
            className="hidden md:inline-flex items-center gap-2 px-4 py-2 bg-primary text-primary-foreground rounded-sm text-sm font-medium hover:opacity-90 transition"
          >
            Get in Touch <ArrowRight className="h-4 w-4" />
          </a>
        </div>
      </header>

      {/* Hero */}
      <section id="top" className="relative min-h-screen flex items-end overflow-hidden">
        <img
          src={heroImg}
          alt="Modern luxury villa at golden hour in the UAE desert"
          className="absolute inset-0 w-full h-full object-cover hero-zoom"
          width={1920}
          height={1280}
        />
        <div className="absolute inset-0 bg-background/70" />
        <div className="absolute inset-0" style={{ background: "var(--gradient-hero)" }} />
        <div className="relative max-w-7xl mx-auto px-6 lg:px-10 pb-24 lg:pb-32 w-full">
          <div className="max-w-3xl">
            <img
              src={logo}
              alt="ArcVera logo"
              className="floaty fade-in delay-300 h-24 w-24 md:h-28 md:w-28 object-cover rounded-sm mb-8 shadow-[var(--shadow-elegant)]"
            />
            <p className="fade-up delay-300 text-primary uppercase tracking-[0.3em] text-xs mb-6">Villa Construction</p>
            <h1 className="fade-up delay-500 font-serif text-5xl md:text-7xl leading-[1.05] mb-8 shimmer-text">
              Crafting timeless villas where construction meets desert serenity.
            </h1>
            <p className="text-lg text-muted-foreground max-w-2xl mb-10">
              ArcVera designs and builds bespoke residences across the U.A.E — meticulously engineered,
              beautifully finished, and grounded in trust.
            </p>
            <div className="flex flex-wrap gap-4">
              <a href="#contact" className="inline-flex items-center gap-2 px-7 py-3.5 bg-primary text-primary-foreground rounded-sm font-medium hover:opacity-90 transition">
                Start your villa <ArrowRight className="h-4 w-4" />
              </a>
              <a href="#services" className="inline-flex items-center gap-2 px-7 py-3.5 border border-border rounded-sm font-medium hover:border-primary hover:text-primary transition">
                Our services
              </a>
            </div>
          </div>
        </div>
      </section>

      {/* About */}
      <section id="about" className="reveal py-28 lg:py-40 px-6 lg:px-10">
        <div className="max-w-7xl mx-auto grid lg:grid-cols-12 gap-12 lg:gap-20">
          <div className="lg:col-span-5">
            <p className="text-primary uppercase tracking-[0.3em] text-xs mb-6">About ArcVera</p>
            <h2 className="font-serif text-4xl md:text-5xl leading-tight">
              A new standard for villa construction craftsmanship in the U.A.E.
            </h2>
          </div>
          <div className="lg:col-span-7 space-y-6 text-muted-foreground text-lg leading-relaxed">
            <p>
              Founded in 2025 in Abu Dhabi, ArcVera was born from a belief that a home should be more
              than built — it should be composed. We partner with discerning owners and investors to
              deliver custom villas that balance architectural clarity, refined materials, and
              uncompromising construction quality.
            </p>
            <p>
              From the first sketch to the final handover, every detail is meticulously managed by our
              in-house team — design, structure, and finishing — ensuring every villa we deliver is a
              quiet statement of modern luxury.
            </p>
            <div className="flex flex-wrap gap-8 pt-6 border-t border-border">
              <div className="flex items-center gap-3">
                <Calendar className="h-5 w-5 text-primary" />
                <span className="text-foreground">Founded 2025</span>
              </div>
              <div className="flex items-center gap-3">
                <MapPin className="h-5 w-5 text-primary" />
                <span className="text-foreground">Abu Dhabi, U.A.E</span>
              </div>
              <div className="flex items-center gap-3">
                <Building2 className="h-5 w-5 text-primary" />
                <span className="text-foreground">Villa Construction</span>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* Services */}
      <section id="services" className="reveal py-28 lg:py-40 px-6 lg:px-10 bg-card">
        <div className="max-w-7xl mx-auto">
          <div className="max-w-3xl mb-16">
            <p className="text-primary uppercase tracking-[0.3em] text-xs mb-6">What We Do</p>
            <h2 className="font-serif text-4xl md:text-5xl leading-tight mb-6">
              Services tailored to every stage of your villa construction.
            </h2>
            <p className="text-muted-foreground text-lg">
              A complete, end-to-end offering — from the first concept to the day you turn the key.
            </p>
          </div>
          <div className="grid md:grid-cols-2 gap-8">
            {[
              {
                img: constructionImg,
                title: "Construction Management",
                body: "Full-scope build oversight with rigorous quality, cost, and timeline control — delivered by an experienced site team.",
              },
              {
                img: interiorImg,
                title: "Interior Finishing",
                body: "Premium joinery, stonework, and lighting curated to bring every interior to a flawless, gallery-grade finish.",
              },
            ].map((s) => (
              <article key={s.title} className="group bg-background border border-border rounded-sm overflow-hidden hover:border-primary transition-all duration-500">
                <div className="aspect-[4/3] overflow-hidden">
                  <img
                    src={s.img}
                    alt={s.title}
                    loading="lazy"
                    width={1280}
                    height={896}
                    className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-700"
                  />
                </div>
                <div className="p-8">
                  <h3 className="font-serif text-2xl mb-3">{s.title}</h3>
                  <p className="text-muted-foreground leading-relaxed">{s.body}</p>
                </div>
              </article>
            ))}
          </div>
        </div>
      </section>

      {/* Numbers */}
      <section id="numbers" className="reveal py-28 lg:py-40 px-6 lg:px-10">
        <div className="max-w-7xl mx-auto">
          <div className="max-w-3xl mb-16">
            <p className="text-primary uppercase tracking-[0.3em] text-xs mb-6">By the Numbers</p>
            <h2 className="font-serif text-4xl md:text-5xl leading-tight">
              Grounded in commitment. Built on detail.
            </h2>
          </div>
          <div className="grid md:grid-cols-3 gap-px bg-border border border-border">
            {[
              { value: "2025", label: "Was Founded" },
              { value: "U.A.E", label: "Headquartered in Abu Dhabi" },
              { value: "100%", label: "Quality Commitment" },
            ].map((n) => (
              <div key={n.label} className="bg-background p-10 lg:p-14">
                <div className="font-serif text-5xl lg:text-6xl text-primary mb-4">{n.value}</div>
                <div className="text-muted-foreground uppercase tracking-wider text-sm">{n.label}</div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Team */}
      <section id="team" className="reveal py-28 lg:py-40 px-6 lg:px-10 bg-card">
        <div className="max-w-7xl mx-auto">
          <div className="max-w-3xl mb-16">
            <p className="text-primary uppercase tracking-[0.3em] text-xs mb-6">Leadership</p>
            <h2 className="font-serif text-4xl md:text-5xl leading-tight mb-6">
              The team behind every ArcVera villa.
            </h2>
            <p className="text-muted-foreground text-lg">
              Meet the Executive Manager and Manager who personally guide every project.
            </p>
          </div>
          <div className="grid md:grid-cols-2 gap-8">
            {[
              {
                img: tamemImg,
                name: "Tamem Mostafa",
                role: "Executive Manager, ARCVERA",
                location: "Abu Dhabi, U.A.E",
              },
              {
                img: mohamdImg,
                name: "Mohamd Adael",
                role: "Manager, ARCVERA",
                location: "Abu Dhabi, U.A.E",
              },
            ].map((p) => (
              <article key={p.name} className="bg-background border border-border rounded-sm overflow-hidden group hover:border-primary transition-all duration-500">
                <div className="aspect-[4/5] overflow-hidden bg-muted">
                  <img
                    src={p.img}
                    alt={`Portrait of ${p.name}`}
                    loading="lazy"
                    className="w-full h-full object-cover group-hover:scale-[1.03] transition-transform duration-700"
                  />
                </div>
                <div className="p-8">
                  <h3 className="font-serif text-2xl mb-1">{p.name}</h3>
                  <p className="text-primary text-sm uppercase tracking-wider mb-4">{p.role}</p>
                  <div className="flex items-center gap-2 text-muted-foreground text-sm">
                    <MapPin className="h-4 w-4" /> {p.location}
                  </div>
                </div>
              </article>
            ))}
          </div>
        </div>
      </section>

      {/* Contact */}
      <section id="contact" className="reveal py-28 lg:py-40 px-6 lg:px-10">
        <div className="max-w-4xl mx-auto text-center">
          <p className="text-primary uppercase tracking-[0.3em] text-xs mb-6">Get in Touch</p>
          <h2 className="font-serif text-4xl md:text-6xl leading-tight mb-8">
            Begin the conversation about your villa.
          </h2>
          <p className="text-muted-foreground text-lg mb-12 max-w-2xl mx-auto">
            Whether you're planning a new residence or refining an existing vision, our team is ready to help.
          </p>
          <div className="grid sm:grid-cols-2 lg:grid-cols-4 gap-6 max-w-4xl mx-auto">
            <a href="mailto:arcvera.uae@gmail.com" className="group flex flex-col items-center gap-3 p-8 border border-border rounded-sm hover:border-primary transition">
              <Mail className="h-6 w-6 text-primary" />
              <span className="text-xs uppercase tracking-wider text-muted-foreground">Email</span>
              <span className="text-sm group-hover:text-primary transition break-all">arcvera.uae@gmail.com</span>
            </a>
            <a href="tel:+9710561121199" className="group flex flex-col items-center gap-3 p-8 border border-border rounded-sm hover:border-primary transition">
              <Phone className="h-6 w-6 text-primary" />
              <span className="text-xs uppercase tracking-wider text-muted-foreground">Phone</span>
              <span className="text-sm group-hover:text-primary transition">+971 056 112 1199</span>
            </a>
            <a href="tel:+9710543272220" className="group flex flex-col items-center gap-3 p-8 border border-border rounded-sm hover:border-primary transition">
              <Phone className="h-6 w-6 text-primary" />
              <span className="text-xs uppercase tracking-wider text-muted-foreground">Phone</span>
              <span className="text-sm group-hover:text-primary transition">+971 054 327 2220</span>
            </a>
            <div className="flex flex-col items-center gap-3 p-8 border border-border rounded-sm">
              <MapPin className="h-6 w-6 text-primary" />
              <span className="text-xs uppercase tracking-wider text-muted-foreground">Location</span>
              <span className="text-sm">Abu Dhabi, U.A.E</span>
            </div>
          </div>
        </div>
      </section>

      {/* Footer */}
      <footer className="border-t border-border py-12 px-6 lg:px-10">
        <div className="max-w-7xl mx-auto flex flex-col md:flex-row items-center justify-between gap-6">
          <div className="flex items-center gap-3">
            <img src={logo} alt="ArcVera" className="h-8 w-8 object-cover rounded-sm" />
            <span className="font-serif text-base">ArcVera General Contracting</span>
          </div>
          <p className="text-sm text-muted-foreground">
            © {new Date().getFullYear()} ArcVera. All rights reserved.
          </p>
        </div>
      </footer>
    </div>
  );
}
@import "tailwindcss" source(none);
@source "../src";
@import "tw-animate-css";

@custom-variant dark (&:is(.dark *));

/*
 * Design system definition.
 *
 * The @theme inline block maps CSS custom properties to Tailwind utility
 * classes (e.g. --color-primary -> bg-primary, text-primary).
 *
 * The :root and .dark blocks define the actual color values using oklch.
 * All colors MUST use oklch format.
 *
 * To add a new semantic color:
 * 1. Add the variable to :root (light value) and .dark (dark value)
 * 2. Register it in @theme inline as --color-<name>: var(--<name>)
 */

@theme inline {
  --radius-sm: calc(var(--radius) - 4px);
  --radius-md: calc(var(--radius) - 2px);
  --radius-lg: var(--radius);
  --radius-xl: calc(var(--radius) + 4px);
  --radius-2xl: calc(var(--radius) + 8px);
  --radius-3xl: calc(var(--radius) + 12px);
  --radius-4xl: calc(var(--radius) + 16px);
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --color-card: var(--card);
  --color-card-foreground: var(--card-foreground);
  --color-popover: var(--popover);
  --color-popover-foreground: var(--popover-foreground);
  --color-primary: var(--primary);
  --color-primary-foreground: var(--primary-foreground);
  --color-secondary: var(--secondary);
  --color-secondary-foreground: var(--secondary-foreground);
  --color-muted: var(--muted);
  --color-muted-foreground: var(--muted-foreground);
  --color-accent: var(--accent);
  --color-accent-foreground: var(--accent-foreground);
  --color-destructive: var(--destructive);
  --color-destructive-foreground: var(--destructive-foreground);
  --color-border: var(--border);
  --color-input: var(--input);
  --color-ring: var(--ring);
  --color-ring-offset-background: var(--background);
  --color-chart-1: var(--chart-1);
  --color-chart-2: var(--chart-2);
  --color-chart-3: var(--chart-3);
  --color-chart-4: var(--chart-4);
  --color-chart-5: var(--chart-5);
  --color-sidebar: var(--sidebar);
  --color-sidebar-foreground: var(--sidebar-foreground);
  --color-sidebar-primary: var(--sidebar-primary);
  --color-sidebar-primary-foreground: var(--sidebar-primary-foreground);
  --color-sidebar-accent: var(--sidebar-accent);
  --color-sidebar-accent-foreground: var(--sidebar-accent-foreground);
  --color-sidebar-border: var(--sidebar-border);
  --color-sidebar-ring: var(--sidebar-ring);
}

:root {
  --radius: 0.25rem;
  --background: oklch(0.16 0.02 250);
  --foreground: oklch(0.96 0.005 80);
  --card: oklch(0.20 0.025 250);
  --card-foreground: oklch(0.96 0.005 80);
  --popover: oklch(0.20 0.025 250);
  --popover-foreground: oklch(0.96 0.005 80);
  --primary: oklch(0.82 0.09 78);
  --primary-foreground: oklch(0.16 0.02 250);
  --secondary: oklch(0.26 0.03 250);
  --secondary-foreground: oklch(0.96 0.005 80);
  --muted: oklch(0.24 0.025 250);
  --muted-foreground: oklch(0.72 0.02 80);
  --accent: oklch(0.82 0.09 78);
  --accent-foreground: oklch(0.16 0.02 250);
  --destructive: oklch(0.577 0.245 27.325);
  --destructive-foreground: oklch(0.984 0.003 247.858);
  --border: oklch(0.30 0.025 250);
  --input: oklch(0.30 0.025 250);
  --ring: oklch(0.82 0.09 78);
  --chart-1: oklch(0.82 0.09 78);
  --chart-2: oklch(0.6 0.118 184.704);
  --chart-3: oklch(0.398 0.07 227.392);
  --chart-4: oklch(0.828 0.189 84.429);
  --chart-5: oklch(0.769 0.188 70.08);
  --sidebar: oklch(0.16 0.02 250);
  --sidebar-foreground: oklch(0.96 0.005 80);
  --sidebar-primary: oklch(0.82 0.09 78);
  --sidebar-primary-foreground: oklch(0.16 0.02 250);
  --sidebar-accent: oklch(0.24 0.025 250);
  --sidebar-accent-foreground: oklch(0.96 0.005 80);
  --sidebar-border: oklch(0.30 0.025 250);
  --sidebar-ring: oklch(0.82 0.09 78);
  --gradient-hero: linear-gradient(180deg, oklch(0.16 0.02 250 / 0.2) 0%, oklch(0.16 0.02 250 / 0.85) 100%);
  --shadow-elegant: 0 30px 80px -20px oklch(0 0 0 / 0.6);
}

.dark {
  --background: oklch(0.129 0.042 264.695);
  --foreground: oklch(0.984 0.003 247.858);
  --card: oklch(0.208 0.042 265.755);
  --card-foreground: oklch(0.984 0.003 247.858);
  --popover: oklch(0.208 0.042 265.755);
  --popover-foreground: oklch(0.984 0.003 247.858);
  --primary: oklch(0.929 0.013 255.508);
  --primary-foreground: oklch(0.208 0.042 265.755);
  --secondary: oklch(0.279 0.041 260.031);
  --secondary-foreground: oklch(0.984 0.003 247.858);
  --muted: oklch(0.279 0.041 260.031);
  --muted-foreground: oklch(0.704 0.04 256.788);
  --accent: oklch(0.279 0.041 260.031);
  --accent-foreground: oklch(0.984 0.003 247.858);
  --destructive: oklch(0.704 0.191 22.216);
  --destructive-foreground: oklch(0.984 0.003 247.858);
  --border: oklch(1 0 0 / 10%);
  --input: oklch(1 0 0 / 15%);
  --ring: oklch(0.551 0.027 264.364);
  --chart-1: oklch(0.488 0.243 264.376);
  --chart-2: oklch(0.696 0.17 162.48);
  --chart-3: oklch(0.769 0.188 70.08);
  --chart-4: oklch(0.627 0.265 303.9);
  --chart-5: oklch(0.645 0.246 16.439);
  --sidebar: oklch(0.208 0.042 265.755);
  --sidebar-foreground: oklch(0.984 0.003 247.858);
  --sidebar-primary: oklch(0.488 0.243 264.376);
  --sidebar-primary-foreground: oklch(0.984 0.003 247.858);
  --sidebar-accent: oklch(0.279 0.041 260.031);
  --sidebar-accent-foreground: oklch(0.984 0.003 247.858);
  --sidebar-border: oklch(1 0 0 / 10%);
  --sidebar-ring: oklch(0.551 0.027 264.364);
}

@layer base {
  * {
    border-color: var(--color-border);
  }

  html {
    scroll-behavior: smooth;
  }

  body {
    background-color: var(--color-background);
    color: var(--color-foreground);
    font-family: 'Inter', system-ui, sans-serif;
    -webkit-font-smoothing: antialiased;
  }

  h1, h2, h3, h4 {
    font-family: 'Playfair Display', Georgia, serif;
    letter-spacing: -0.02em;
  }
}

@layer utilities {
  .fade-up { animation: fadeUp 1s ease-out both; }
  .fade-in { animation: fadeIn 1.2s ease-out both; }
  .delay-100 { animation-delay: 0.1s; }
  .delay-200 { animation-delay: 0.2s; }
  .delay-300 { animation-delay: 0.3s; }
  .delay-500 { animation-delay: 0.5s; }
  .delay-700 { animation-delay: 0.7s; }

  .reveal { opacity: 0; transform: translateY(40px); transition: opacity 0.9s ease-out, transform 0.9s ease-out; }
  .reveal.is-visible { opacity: 1; transform: translateY(0); }

  .hero-zoom { animation: heroZoom 12s ease-out both; }
  .floaty { animation: floaty 6s ease-in-out infinite; }
  .shimmer-text {
    background: linear-gradient(90deg, var(--foreground) 0%, var(--primary) 50%, var(--foreground) 100%);
    background-size: 200% 100%;
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    animation: shimmer 6s linear infinite;
  }

  .splash-logo { animation: splashLogo 1.6s cubic-bezier(0.22, 1, 0.36, 1) both; }
  .splash-ring { animation: splashRing 2s ease-out infinite; }
  .splash-text { animation: fadeUp 1s ease-out 0.4s both; }
  .splash-fade-out { animation: splashOut 0.7s ease-in both; }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(24px); }
    to { opacity: 1; transform: translateY(0); }
  }
  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }
  @keyframes heroZoom {
    from { transform: scale(1.15); }
    to { transform: scale(1); }
  }
  @keyframes floaty {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-8px); }
  }
  @keyframes shimmer {
    from { background-position: 200% 0; }
    to { background-position: -200% 0; }
  }
  @keyframes splashOut {
    to { opacity: 0; visibility: hidden; }
  }
  @keyframes splashLogo {
    0% { opacity: 0; transform: scale(0.7) rotate(-8deg); }
    60% { opacity: 1; transform: scale(1.05) rotate(2deg); }
    100% { opacity: 1; transform: scale(1) rotate(0); }
  }
  @keyframes splashRing {
    0% { transform: scale(0.8); opacity: 0.8; }
    100% { transform: scale(1.6); opacity: 0; }
  }
}
